from prompt_file import guardar_prompt
from prompt_file import cargar_lista_prompt
from tokens import calcular_total_pagar
import pandas as pd

# guardar_prompt(prompt)

lista = cargar_lista_prompt()

print(lista)

# lista.to_excel("versiones.xlsx")
